{
  "scripts": {
    "prepare": "husky install"
  },
  "devDependencies": {
    "@prettier/plugin-ruby": "^2.1.0",
    "husky": "^7.0.4",
    "lint-staged": "^12.3.8",
    "prettier": "^2.6.2"
  },
  "lint-staged": {
    "**/*": "prettier --write --ignore-unknown"
  }
}
